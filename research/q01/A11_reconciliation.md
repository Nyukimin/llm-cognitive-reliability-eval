# A11 FaithEval：GitHub版と配布版の統合処置

処置日：2026-09-21。対象は成果物の相違と状態管理であり、A11の全データ再調査ではない。

## 正本と旧パッチ

正本はmainにあるA11一次精査記録と、この統合記録。旧パッチ `5678684125fa…` は**適用不要・適用対象外**とする。別内容の既存6ファイルへ重ねて適用しない。

既存GitHub版 `f7e5c3b7337059f587e9d54cb4e1d0f09c65d450` の精査本文・データ記録・独立検査コード・検査結果は変更しない。配布版6ファイルと付属記録は [archives](../../archives/a11_patch_5678684125fa/README.md) に元のバイト列のまま保存した。進捗・索引に今回の統合への参照を追加する。元の要求仕様v0.5、Stage 1、A04〜A10は変更しない。

この処置は旧パッチをそのまま採用したものでも、二つの版を同一だったと書き換えたものでもない。旧パッチの基点は `90289d4…`、今回の基点は `f7e5c3b…`。ファイルごとのGit blob・SHA256・採否は [対応台帳](../../data/q01/A11_reconciliation.json) に残す。

## 相違ごとの採否

| 相違 | 処置 |
|---|---|
| Git版は独立した等価実装、配布版は固定READMEの関数・判定式を抽出 | 両実装を残し、追加検査で同じ入力に対する出力を照合する。旧CLIは変更しない。README全体、モデル・データのロード部分は実行しない |
| 同じU01〜I06でも入力例が異なる | `git:U01`、`package:U01`等に分離。入力例を上書きせず、同一課題・応答組の重複も明示する |
| 配布版だけにある回答不能の表示例2件 | ID `4d34e1d759844a3a8b60fe75199310e2` と `bd4ac80e6ab34822bd30bc62c0aa84f0` を追加の再確認候補として保持。「当該配布文書が全文表示を読んだと記録している」という出所に限定し、今回全文取得・ラベル承認したとはしない |
| 配布版のParquetハッシュ | 元JSONに保持。配布ページ由来とされた値であり、今回データ全バイトと照合した値ではない |
| B14をdirect_narrowとするかpartialとするか | 資料不足・競合の認識という狭い範囲の候補。確認質問・コスト等まで含むB14全体としては部分対応を維持 |
| 同じqidが異なる課題に存在 | 資産・課題・版・qidで識別し、元素材の共有を独立観測に数えない条件を明示して保持 |
| 進捗・適用案内の「未反映」 | 元資料では当時の記述として保存し、現在の操作指示から切り離す。今後の作業はmainの索引を使う |

公開原データ・日本語翻訳の権利は引き続き確認待ち。元の部分一致処理を正式な意味採点に採用しない判断、回答可能・非矛盾の対照、入力とラベルの分離は維持する。今回原資料を再取得していないプレビューやハッシュを「確認済み」に昇格させない。

## 実行した検査

固定READMEをGitHub経由で再取得し、11,208 bytes、Git blob `78e5de53ae807484dc40f96fa2a146e4301f3826` をローカル入力と照合した。両方の元検査を再実行し、Git版の12入力と配布版の12入力が、それぞれ保存されていた結果とバイト単位で一致した。

次に両版の全入力を、固定READMEから抽出した処理とGit版の独立実装の両方へ与えた。24の出所付きエントリ、22種類の課題・応答組、非strict/strictの48比較で不一致0。改変README、改変した両検査コード、未対応課題等の5つの負例も拒否した。詳細は [再検査結果](../../data/q01/A11_reconciliation_checks.json)。

```text
python scripts/q01/check_faitheval_contract.py
python scripts/q01/check_faitheval_reconciliation.py PATH_TO_PINNED_UPSTREAM_README_MD
```

上流READMEはリポジトリに複製しない。取得する版はSalesforceAIResearch/FaithEvalの `58d35840e3fbc7bf4b7672582a417b3b3a327dec`。検査は入力のGit blob一致を要求する。アーカイブ内の旧適用手順は実行しない。

さらにローカルの限定作業ツリーで元パッチを逆適用し、基点2ファイルのGit blobを照合した後、再適用して全6ファイルの元バイト列を復元できることを確認した。二重適用の拒否も確認した。これは現在のmainへのパッチ適用ではない。

これらは成果物・採点処理の局所検証。全入力に対する実装同値の証明、LLMの性能、人手ラベルの妥当性、実データ上の誤判定率を示さない。24エントリや48比較を独立したモデル試行として数えない。

## 過去の操作について残る未解明事項

二つの成果物があることと、その差分は確認できた。一方、`f7e5c3b` を公開した具体的なツール要求や、最初の未反映報告時点にmainがどこを指していたかをつなぐ記録は未特定。現在の存在やコミットのauthor名から、操作主体・経路を推定しない。

今回の統合で成果物の二重管理は処置するが、過去の原因調査を「解決済み」にしない。再実行で旧結果を再現できたことも、当時どの処理が実行されたかの証明ではない。

## 進捗

A11は資産・プロトコルの一次精査まで。正式Full採用0、正式Short採用0、対象モデル実行0を維持する。次の資産はA17。資料全取得、個別ラベル、公開許諾、手動経路、正式採点の受入れは既存の残課題として残す。今回その調査を開始・完了したという扱いにしない。
